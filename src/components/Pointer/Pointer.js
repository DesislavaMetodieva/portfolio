
import { init_pointer } from "https://seattleowl.com/pointer.js/pointer.js";

// init_pointer({
//   pointerColor: "#00ffa8", // Css color
//   ringSize: 15,
//   ringClickSize: 10
// });

const Pointer = () => {
    init_pointer({
        pointerColor: "#00ffa8", // Css color
        ringSize: 15,
        ringClickSize: 10
      });
    return (
        <>
       </>
         
    );
  };
  
  export default Pointer;
